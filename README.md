# AI-AGENTS

Next.js 14 (App Router) + TypeScript + Tailwind CSS scaffold.

## Getting started

```bash
pnpm install
pnpm dev
```

- Landing page: http://localhost:3000
- Protected layout shell: http://localhost:3000/dashboard

Copy `.env.example` to `.env.local` and fill in values as needed.
